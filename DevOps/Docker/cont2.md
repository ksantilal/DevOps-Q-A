Our first Docker work - Containerize your application:
--------------------------------------------------------------

* This involes building a docker image 
* Once we have build the docker image we store in it registry
* We can create container from docker image

* Terms
    * Host: The Machine (Physical/Virtual)
    * Docker image:
    * Container:
    * Registry: 

## Docker Components: 
* This is primary & simple version
* When we install docker, we get two major components
    * Client
    * Server/engine
* Client: This is the command line tool which we use to interact with docker engine
  - docker info 

* Virtual machine providing the hardware resources.
